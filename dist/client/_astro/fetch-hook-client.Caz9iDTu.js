import { a as T } from "./js.cookie.Cz0CWeBA.js"; async function w({ url: o, method: f = "GET", body: n, token: p, init: s, baseUrl: d, timeout: m = 15e3 }) { const h = d ?? "https://admin-osama.cashierthru.com/api/", i = o.startsWith("http") ? o : `${h}${o.startsWith("/") ? o : `${o}`}`; console.log("fullUrl:", i); const c = p ?? T.get("app_token") ?? void 0, g = { "Content-Type": "application/json", ...s?.headers || {}, ...c ? { Authorization: `Bearer ${c}` } : {} }, l = new AbortController, u = setTimeout(() => l.abort(), m); try { const e = await fetch(i, { method: f, body: n ? JSON.stringify(n) : void 0, ...s, headers: g, signal: l.signal }); clearTimeout(u); const { status: r, ok: a } = e, y = e.headers.get("content-type") ?? ""; let t = null; if (r !== 204) if (y.includes("application/json")) try { t = await e.json() } catch { t = null } else try { t = await e.text() } catch { t = null } if (!a) { const b = typeof t == "object" && t && "message" in t && t.message || typeof t == "object" && t && "error" in t && t.error || typeof t == "string" && t || e.statusText || "Request failed"; return { data: null, status: r, ok: a, error: String(b) } } return { data: t ?? null, status: r, ok: a } } catch (e) { return clearTimeout(u), e instanceof Error && e.name === "AbortError" ? { data: null, status: 408, ok: !1, error: "Request timeout" } : { data: null, status: 0, ok: !1, error: e instanceof Error ? e.message : "Network error" } } } export { w as f };
