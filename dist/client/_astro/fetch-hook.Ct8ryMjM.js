function g() { return "https://admin-emend.cashierthru.com" } async function T({ url: i, init: o, token: c, timeoutMs: d = 15e3, baseUrl: m }) { const f = g(), l = `${m ?? `${f}/api/`}${i}`; console.log("fullUrl", l); const u = new AbortController, h = setTimeout(() => u.abort(), d); try { const r = { ...o?.body instanceof FormData ? {} : { "Content-Type": "application/json" }, ...c ? { Authorization: `Bearer ${c}` } : {}, ...o?.headers ?? {} }, e = await fetch(l, { ...o, method: o?.method ?? "GET", headers: r, signal: u.signal }), { status: a, ok: s } = e, p = e.headers.get("content-type") ?? ""; let t = null; if (a !== 204) if (p.includes("application/json")) try { t = await e.json() } catch { t = null } else try { t = await e.text() } catch { t = null } if (!s) { const y = typeof t == "object" && t && "message" in t && t.message || typeof t == "object" && t && "error" in t && t.error || typeof t == "string" && t || e.statusText || "Request failed"; return { data: null, status: a, ok: s, error: String(y) } } return { data: t ?? null, status: a, ok: s } } catch (n) { const r = n?.name === "AbortError"; return { data: null, status: r ? 408 : 500, ok: !1, error: r ? "Request timed out" : n?.message ?? "Unknown error" } } finally { clearTimeout(h) } } export { T as f };
